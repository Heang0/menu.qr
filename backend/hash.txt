const bcrypt = require("bcrypt");
const password = "_______";
bcrypt.hash(password, 10, (err, hash) => {
  if (err) throw err;
  console.log(hash);
});


# then run 
# node hash.js
# and paste hashpw in mongodb